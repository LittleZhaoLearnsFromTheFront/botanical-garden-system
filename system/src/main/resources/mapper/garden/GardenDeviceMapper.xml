<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.garden.GardenDeviceMapper">

    <resultMap type="GardenDevice" id="GardenDeviceResult">
        <id     property="deviceId"     column="device_id"    />
        <result property="areaId"       column="area_id"      />
        <result property="areaName"     column="area_name"    />
        <result property="deviceName"   column="device_name"  />
        <result property="deviceCode"   column="device_code"  />
        <result property="deviceType"   column="device_type"  />
        <result property="manufacturer" column="manufacturer" />
        <result property="installDate"  column="install_date" />
        <result property="usageCount"   column="usage_count"  />
        <result property="status"       column="status"       />
        <result property="delFlag"      column="del_flag"     />
        <result property="createBy"     column="create_by"    />
        <result property="createTime"   column="create_time"  />
        <result property="updateBy"     column="update_by"    />
        <result property="updateTime"   column="update_time"  />
        <result property="remark"       column="remark"       />
    </resultMap>

    <sql id="selectDeviceVo">
        select d.device_id, d.area_id, a.area_name, d.device_name, d.device_code, d.device_type, d.manufacturer,
               d.install_date, d.usage_count, d.status, d.del_flag, d.create_by, d.create_time, d.update_by, d.update_time, d.remark
        from garden_device d
        left join garden_area a on a.area_id = d.area_id and a.del_flag = '0'
    </sql>

    <select id="selectDeviceById" parameterType="Long" resultMap="GardenDeviceResult">
        <include refid="selectDeviceVo"/>
        where d.device_id = #{deviceId}
    </select>

    <select id="selectDeviceList" parameterType="GardenDevice" resultMap="GardenDeviceResult">
        <include refid="selectDeviceVo"/>
        where d.del_flag = '0'
        <if test="deviceName != null and deviceName != ''">
            AND d.device_name like concat('%', #{deviceName}, '%')
        </if>
        <if test="deviceCode != null and deviceCode != ''">
            AND d.device_code like concat('%', #{deviceCode}, '%')
        </if>
        <if test="deviceType != null and deviceType != ''">
            AND d.device_type = #{deviceType}
        </if>
        <if test="areaId != null">
            AND d.area_id = #{areaId}
        </if>
        <if test="status != null and status != ''">
            AND d.status = #{status}
        </if>
        order by d.device_id desc
    </select>

    <insert id="insertDevice" parameterType="GardenDevice" useGeneratedKeys="true" keyProperty="deviceId">
        insert into garden_device(
            <if test="areaId != null">area_id,</if>
            <if test="deviceName != null and deviceName != ''">device_name,</if>
            <if test="deviceCode != null and deviceCode != ''">device_code,</if>
            <if test="deviceType != null and deviceType != ''">device_type,</if>
            <if test="manufacturer != null and manufacturer != ''">manufacturer,</if>
            <if test="installDate != null">install_date,</if>
            <if test="usageCount != null">usage_count,</if>
            <if test="status != null and status != ''">status,</if>
            <if test="remark != null and remark != ''">remark,</if>
            <if test="createBy != null and createBy != ''">create_by,</if>
            create_time
        )values(
            <if test="areaId != null">#{areaId},</if>
            <if test="deviceName != null and deviceName != ''">#{deviceName},</if>
            <if test="deviceCode != null and deviceCode != ''">#{deviceCode},</if>
            <if test="deviceType != null and deviceType != ''">#{deviceType},</if>
            <if test="manufacturer != null and manufacturer != ''">#{manufacturer},</if>
            <if test="installDate != null">#{installDate},</if>
            <if test="usageCount != null">#{usageCount},</if>
            <if test="status != null and status != ''">#{status},</if>
            <if test="remark != null and remark != ''">#{remark},</if>
            <if test="createBy != null and createBy != ''">#{createBy},</if>
            sysdate()
        )
    </insert>

    <update id="updateDevice" parameterType="GardenDevice">
        update garden_device
        <set>
            <if test="areaId != null">area_id = #{areaId},</if>
            <if test="deviceName != null and deviceName != ''">device_name = #{deviceName},</if>
            <if test="deviceCode != null">device_code = #{deviceCode},</if>
            <if test="deviceType != null">device_type = #{deviceType},</if>
            <if test="manufacturer != null">manufacturer = #{manufacturer},</if>
            <if test="installDate != null">install_date = #{installDate},</if>
            <if test="usageCount != null">usage_count = #{usageCount},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
            update_time = sysdate()
        </set>
        where device_id = #{deviceId}
    </update>

    <delete id="deleteDeviceById" parameterType="Long">
        update garden_device set del_flag = '2' where device_id = #{deviceId}
    </delete>

    <delete id="deleteDeviceByIds" parameterType="Long">
        update garden_device set del_flag = '2' where device_id in
        <foreach item="deviceId" collection="array" open="(" separator="," close=")">
            #{deviceId}
        </foreach>
    </delete>

</mapper>

